import tw from "tailwind-styled-components";
import styled from "styled-components";

function Schedule() {
  return (
    <div>
      <span className="text-xl sm:text-2xl font-extrabold">선지원 후고민, 아시죠?</span>

      <img className="mt-5" src="일정.png"></img>
    </div>
  );
}

export default Schedule;
